{% extends "home/layout.njk" %}

{% block head %}
    <title>Foodfy - Receitas</title>
    <link rel="stylesheet" href="/home/recipes.css">
    <link rel="stylesheet" href="/home/paginate.css">
{% endblock %} 

{% block content %}
    <div class="receitas">                
        <div class="cards">
        {%for recipe in recipes %}
            <div class="card" id="{{recipe.id}}">
                <img class="card-img" src="{{files[loop.index0].src}}" alt="{{recipe.title}}">
                <h4 class="card-titulo">{{recipe.title}}</h4>
                <small class="card-autor">por {{recipe.author}}</small>
            </div>
        {% endfor %}
    </div>

    <div class="pagination" data-page={{pagination.page}} data-total={{pagination.total}} data-filter={{filter}}></div>

<script src="/home/paginate.js"></script>
<script src="/home/recipes.js"></script>
{% endblock %} 
